Large-title header — ink on the page background (never a colored band), rounded display face, sticky.

```jsx
<AppHeader title="Ms. Rivera's Class" subtitle="2025–2026 · 24 students" action={<IconButton ariaLabel="Cancel" icon={<Icon name="x" size={18} />} />} />
```
