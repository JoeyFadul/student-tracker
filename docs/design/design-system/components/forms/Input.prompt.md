Text input — borderless soft-gray fill, 12px radius, no focus ring. Also exports `Label` and `inputBaseStyle`.

```jsx
<Input label="Student name" placeholder="e.g. Maya Rodriguez" value={v} onChange={e => setV(e.target.value)} />
```
