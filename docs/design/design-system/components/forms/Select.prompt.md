Native select with soft-fill styling and inline chevron — the app deliberately keeps real system pickers (accessibility).

```jsx
<Select label="Grade" options={[{value:'K',label:'Kindergarten'},{value:'1',label:'1st grade'}]} />
```
