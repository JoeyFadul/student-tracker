Multiline input — same soft-gray fill as Input. Used for notes and custom reasons (50-char cap with a live counter in the app).

```jsx
<Textarea label="Notes" placeholder="Type a reason…" rows={3} maxLength={50} />
```
