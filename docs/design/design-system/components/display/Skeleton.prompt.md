Shimmer skeletons — the rule: skeletons for loading content, spinners only inside busy controls. `SkeletonList` mirrors the roster rows.

```jsx
<SkeletonList count={5} />
<Skeleton width="55%" height={14} />
```
