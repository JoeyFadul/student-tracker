Student avatar squircle — photo if set, emoji otherwise, 🌱 default, on a honey-soft tile. Every roster/profile/sheet surface uses this.

```jsx
<Avatar student={{ name: 'Maya', photo: '🦊' }} size={56} emojiSize={30} />
<Avatar student={maya} size={120} radius={32} emojiSize={56} />
```
