Card-style empty state — icon in a soft terracotta circle, calm sentence-case copy, one action.

```jsx
<EmptyState icon={<Icon name="calendar" size={32} color="var(--wd-accent)" />} title="No active school year" hint="Start a year to begin awarding points." action={<Button>Go to settings</Button>} />
```
