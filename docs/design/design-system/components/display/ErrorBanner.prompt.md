Inline error strip at the top of content — soft danger fill, × to dismiss. Null-safe: no message, no render.

```jsx
<ErrorBanner message="Couldn't load students. Pull to retry." onDismiss={clear} />
```
