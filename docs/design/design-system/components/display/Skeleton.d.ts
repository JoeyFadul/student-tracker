/** Shimmer loading block. Also exports SkeletonRow (student-row silhouette) and SkeletonList. */
export interface SkeletonProps {
  width?: number | string;
  height?: number | string;
  radius?: number;
  style?: React.CSSProperties;
}
