Lucide line icon by name — the only icon system in Well Done (no icon fonts, no PNGs, no decorative emoji).

```jsx
<Icon name="flame" size={14} color="var(--wd-danger)" />
<Icon name="users" size={24} strokeWidth={2.4} />
```

Names available: users, bar-chart-3, settings (tab bar); flame (streaks); plus, minus, check, x; chevron-left/right/down; search, arrow-up-down; camera, more-horizontal, log-out, trending-up, alert-triangle, loader-2 (add className "spin"), calendar, trash-2, pencil, user-plus, mail.
